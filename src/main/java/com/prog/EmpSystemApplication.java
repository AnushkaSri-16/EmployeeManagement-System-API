package com.prog;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
@SpringBootApplication
public class EmpSystemApplication {

	public static void main(String[] args) {
		SpringApplication.run(EmpSystemApplication.class, args);
	}

}

//The main() method in the EmpSystemApplication class is the entry point for the application. 
//The method calls the SpringApplication.run() method to start the Spring Boot application.
//it  sets up a Spring Boot application for an Employee Management System and starts the application.